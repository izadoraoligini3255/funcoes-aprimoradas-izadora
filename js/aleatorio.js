 const nomes =["julia", "maria", "Bruna", "Joana", " Felipe", "Gabriel"];
 
 
 export function aleatorio(lista){
    const posicao = Math.floor(Math.ramdon()*lista.length);
    return lista[posicao];
}

export conts nome = aleatorio(nomes);